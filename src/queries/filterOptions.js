export const cities = `
   query {
       allCities {
         id,
         name
       }
   }
 `

export const practiceTypes = `
   query {
       allPracticeTypes {
         id,
         name
       }
   }
 `

export const insuranceTypes = `
   query {
       allInsurances {
         id,
         name
       }
   }
 `

export const searchPageFiltersOptions = `
   query {
       allCities {
         id,
         name
       }
       allPracticeTypes {
         id,
         name
       }
       allInsurances {
         id,
         name
       }
   }
 `
